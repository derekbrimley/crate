{
  "framework": "vite",
  "buildCommand": "tsc -p tsconfig.server.json && find api lib -name '*.ts' -delete && vite build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/((?!api(?:/|$)|@)[^.]*)", "destination": "/index.html" }]
}
